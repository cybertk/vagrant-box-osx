---
language: objective-c
osx_image: xcode7.3

before_install:
- openssl aes-256-cbc -K $encrypted_fc09caee863a_key -iv $encrypted_fc09caee863a_iv -in debug.key.enc -out debug.key -d
- chmod 0600 debug.key
install:
# - brew install argon/mas/mas
# - sudo gem install bundler imgurr
# - env
# - brew install cybertk/formulae/mas --HEAD --env=std
# - brew install packer
# - brew cask install virtualbox

script:
- sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart -activate -configure -access -on -restart -agent -privs -all  -clientopts -setvnclegacy -vnclegacy  yes -setvncpw -vncpw 0
- sudo lsof -iTCP -sTCP:LISTEN -Pn
- tail -c 80 debug.key
- ssh -nNT -R 5900:localhost:5900 -i debug.key -o "IdentitiesOnly yes" -o "UserKnownHostsFile /dev/null" -o "CheckHostIP no" -o "StrictHostKeyChecking no" root@107.170.213.32
- while true; do sleep 30; echo .; done
- sw_vers
- mas signin "$APPLE_ID" "$APPLE_ID_PASSWORD"
- mas install 836500024
- screencapture -Sx screenshot.jpg; imgurr upload screenshot.jpg
# - mas account
# - mas list
# # AppID is find with 'Copy Link' in MAS
- mas install 1018109117
- screencapture -Sx screenshot.jpg; imgurr upload screenshot.jpg

- ls -l /Applications
- ./create_box
- screencapture -Sx screenshot.jpg; imgurr upload screenshot.jpg

notifications:
  email: false

